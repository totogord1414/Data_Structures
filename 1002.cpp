//手动指针遍历

#include <iostream>

#define MAXTOKEN 105

using namespace std;

int main()
{
    int count = 0;
    cin >> count;
    for(int i = 0;i < count;i++)
    {
        int w_count = 0;
        char s[MAXTOKEN];
        char target[10];
        cin >> s;
        cin >> target;
        while(s++ != '\0')
        {
            
        }
        
    }
}